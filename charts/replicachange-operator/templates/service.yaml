apiVersion: v1
kind: Service
metadata:
  name: {{ include "replicachange-operator.fullname" . }}
  labels:
    {{- include "replicachange-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "replicachange-operator.selectorLabels" . | nindent 4 }}
