---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: replicationchanges.core.my.domain
spec:
  group: core.my.domain
  names:
    kind: ReplicationChange
    listKind: ReplicationChangeList
    plural: replicationchanges
    singular: replicationchange
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: ReplicationChange is the Schema for the replicationchanges API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ReplicationChangeSpec defines the desired state of ReplicationChange
            properties:
              replicas:
                description: Foo is an example field of ReplicationChange. Edit replicationchange_types.go
                  to remove/update
                format: int32
                type: integer
            type: object
          status:
            description: ReplicationChangeStatus defines the observed state of ReplicationChange
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
